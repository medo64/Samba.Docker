[global]
   workgroup = WORKGROUP
   security = user
   map to guest = Bad User
   guest account = nobody

[Test]
   path = /share/test/
   browsable = yes
   writable = yes
   guest ok = yes
   force user = nobody
